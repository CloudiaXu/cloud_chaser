{
  "name": "cloud-chaser",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "npm run articles && vite",
    "build": "npm run articles && npm run og && tsc && vite build",
    "preview": "vite preview",
    "articles": "node scripts/build_articles.mjs",
    "bird": "node scripts/build_bird_svg.mjs",
    "og": "npm run bird && cd public && rsvg-convert -w 1200 -h 630 og-image.svg -o og-image.png",
    "_dev_note": "articles runs first so dialog HTML is injected into index.html before vite dev/build picks it up"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "marked": "^18.0.3",
    "pngjs": "^7.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
