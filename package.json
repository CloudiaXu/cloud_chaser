{
  "name": "cloud-chaser",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "bird": "node scripts/build_bird_svg.mjs",
    "og": "npm run bird && cd public && rsvg-convert -w 1200 -h 630 og-image.svg -o og-image.png"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "pngjs": "^7.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
